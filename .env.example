# No required variables. Ports are published plainly (see docker-compose.yml);
# restrict with the host firewall if wanted, and set the SRT/TCP passphrases
# in the admin when the ingests face the internet.
