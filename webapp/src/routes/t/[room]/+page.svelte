<script>
	// A scoped theater: /t/<room> is the same page as the front door — the
	// room comes from the URL (api.currentChannel reads location), so the
	// status poll, HLS source, and chat socket all follow it.
	import Page from '../../+page.svelte';
</script>

<Page />
